import CardButton from '../CardButton';
import './JournalAddButton.css';

function JournalListAddButton() {
    return (
        <CardButton className="journal-add">
            + Новое воспоминание
        </CardButton>
    );
}

export default JournalListAddButton;